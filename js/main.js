/* =============================================
   JUNAI BHAIMIA UNIVERSITY — MAIN ENTRY POINT
   Initializes all page scripts when DOM is ready.
   ============================================= */

(function () {
    'use strict';

    // Wait for DOM to fully load before running scripts
    document.addEventListener('DOMContentLoaded', function () {
        console.log('JBU Website Loaded Successfully');

        // All individual modules are loaded as separate <script> tags
        // in the HTML file and run immediately via their self-executing functions.
        // This main file handles any cross-module initialization here.
    });
})();
